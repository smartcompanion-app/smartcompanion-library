# Prettier resolves .prettierignore relative to its cwd, and the format scripts
# run inside this package, so the root .prettierignore does not apply here.
#
# Both files are regenerated by Stencil on every build, in Stencil's own style.
# Formatting them means the next build reintroduces a format:check failure.
src/components.d.ts
custom-elements.json
